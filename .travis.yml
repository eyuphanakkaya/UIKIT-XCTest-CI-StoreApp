language: swift
os: osx
osx_image: xcode14.3

script:
- xcodebuild clean test \
      -project Store-App.xcodeproj \
      -scheme "CI" \
      -destination "platform=iOS Simulator,OS=18.2,name=iPhone SE (3rd generation)" \
      -testPlan CITestPlan \
      CODE_SIGN_IDENTITY="" \
      CODE_SIGNING_REQUIRED=NO \
      ONLY_ACTIVE_ARCH=NO
